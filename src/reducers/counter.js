const counterReducer = (state="",action) =>{
    switch (action.type) {
        case "SET-EXP":
            return state;
        default:
            return state;
    }
}

export default counterReducer;
